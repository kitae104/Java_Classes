package part1.collections.dlist;

public class MyDLinkedList {

    private DLNode DL = null;
    public void insertNode(String data) {
        DLNode newNode = new DLNode(data);
        // 처음 넣는 경우
        if(DL == null){

        } else {

        }

        // 기존에 있는데 넣는 경우

    }

    public void printDList() {
    }
}
